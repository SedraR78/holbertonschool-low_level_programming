#include <stdlib.h>
#include <string.h>
#include "lists.h"


dlistint_t *add_dnodeint(dlistint_t **head, const int n)
{
    dlistint_t new; 

    new = malloc(sizeof(list_t));
 if (!new)
 return (NULL);

}
